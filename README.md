# BILI PLUS

## 功能介绍

本项目是用于在 PC 浏览器使用 B 站时，作为谷歌浏览器插件，强化各种功能特性或提供额外的功能。
目前主要是提供字幕额外功能，可自动提取当前 B 站视频字幕并下载为为含时间戳的`.srt`字幕文件和借助AI模型（需要自行寻找和提供模型服务）基于字幕完成视频总结。
还可以启动AI助手执行指定任务。

## 待实现
2. 评论区用户IP地区显示，从评论接口里进行拦截获取到地区并显示到对应的评论区。
3. 页面里直接显示下载字幕、下载视频、下载音频等增强按钮。

## 使用方法

1. 打开 Chrome → `chrome://extensions/`
2. 启用“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择本目录
5. 打开任意 B 站视频详情页
6. 点击插件图标 → 找到按钮“提取字幕”

即可下载对应的字幕文件到当前设备。

## 功能

-   自动读取登录 Cookie（无需手动输入）
-   自动识别可用字幕语言
-   一键导出 `.srt` 文件

## 目录结构

* src/entry/background - 插件后台脚本，用于实现统一的任务处理
* src/entry/content - 插件内容脚本，主要用于嫁接页面与插件的通讯
* src/entry/biliVideo - 页面注入脚本，主要用于获取页面信息和提供侵入式交互
* src/entry/popup - 插件弹出页面脚本，主要用于完成配置、设置
* src/entry/sidepanel - 插件侧边栏脚本，用户进行输入输出的主要界面